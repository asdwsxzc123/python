线性表
1. 顺序表,连续的
2. 链表,用线串起来

链接区保存的是下一个节点的地址
单链表操作
is_empty() 链表是否为空
length() 链表的长度
travel() 遍历整个链表
add(item) 链表头部添加元素
append(item) 链表尾部添加
insert(pos, item) 指定位置添加
remove(item) 删除节点
search(item) 查找节点是否存在